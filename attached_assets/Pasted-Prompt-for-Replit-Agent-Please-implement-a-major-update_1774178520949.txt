Prompt for Replit Agent:"Please implement a major update to the Budget Tracker's core logic and UI. We are moving from a static daily limit to a Dynamic Cumulative Budgeting System.1. New State & Settings RequirementsAdd the following fields to the user settings and ensure they are synced with the database (Firebase):incomeAmount (number): The amount of money received.incomePeriod (number): The period in days (e.g., 7 for weekly, 30 for monthly).autoRollover (boolean): A toggle to enable/disable transferring remainders to the next day.accumulatedBalance (number): This is the 'k' variable. It stores the total sum of all previous days' unspent (or overspent) money.2. The Mathematical CoreImplement these formulas for the budget calculation:Base Daily Income ($I$): I = (incomeAmount * (365 / incomePeriod)) / 365(This ensures the daily rate is accurate regardless of the period chosen).Today's Total Limit ($a$): a = I + accumulatedBalanceDaily Delta ($m$): At the end of the day, calculate m = a - todaySpending.The Carry-Over Logic: If autoRollover is ON, at 00:00 (midnight), update accumulatedBalance by adding m to it. Then reset todaySpending to 0.3. UI/UX Update (The 'Degree' Feature)Update the main dashboard (Home.tsx) to show the budget in a gamified way:Main Display: Show Today's Total Limit (a) as the large primary number.Superscript (The 'Degree'): Directly above/to the right of the main number, add a small indicator showing the accumulatedBalance.If accumulatedBalance is positive: Show as +X in Green (e.g., $180^{+30}$).If accumulatedBalance is negative: Show as -X in Red (e.g., $135^{-15}$).Use the <sup> HTML tag or equivalent CSS positioning.Secondary Info: Below the main number, show the Base Daily Income (I) in a smaller font (label it 'Base Daily').4. Settings ViewCreate a clean input for incomeAmount and incomePeriod.Add the autoRollover toggle with a description: 'Transfer yesterday's remainder to today'.Important: Add safety checks to prevent division by zero if incomePeriod is empty or 0. Make sure the 'Degree' updates in real-time as the user changes settings."